<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AllGoods">
        <activity
            android:name=".UI.Customer.ProductDetails.ProductDetails"
            android:exported="false" />
        <activity
            android:name=".UI.Auth.forgetpassword.NewPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Auth.forgetpassword.VerificationActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Auth.signup.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Auth.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Auth.forgetpassword.ForgetPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Main.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.Main.MainActivity"
            android:exported="true" />
    </application>

</manifest>